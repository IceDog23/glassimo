{%- assign headerSnippetPath = "header-index" -%}{%- assign wfPage = "65397da03766491d0cad2175" -%}{%- assign bodyClasses = "" -%}
  {%- section 'custom_marquee' -%}
  {%- render 'navigation-1' -%}
  {%- section 'slider-options' -%}
  <section class="product-sections">
    <div class="section-wrapper in-product-list">
      <div class="text-flex">
        <div class="h2">{{ 't1424c2f2' | t }}</div>
        <a data-w-id="0118116c-0090-bd4e-123c-2d05d503a665" href="/collections/all" class="page-link text-flex w-inline-block">
          <div class="h3 main">{{ 'tn1084d40d' | t }}</div>
          <div data-w-id="6a3276da-d715-78ae-eb19-bdbf8e77ca2f" class="icon-embed-xxsmall arrow-right w-embed"><svg xmlns="http://www.w3.org/2000/svg" width="100%" height="100%" viewBox="0 0 16 16" fill="none" preserveAspectRatio="xMidYMid meet" aria-hidden="true" role="img">
              <path d="M6 12L10 8L6 4" stroke="currentColor" stroke-linecap="round" stroke-linejoin="round"></path>
            </svg></div>
        </a>
      </div>
      {%- if settings.collection_home != blank -%}{%- render "markup_for_collection_home" -%}{%- else -%}{%- render "produkty_max_4_sorted_by_created_on_reversed" -%}{%- endif -%}
    </div>
  </section>
  <div class="divider in-decor">
    <div class="divider-line"></div>
  </div>
  <section class="designers-section">
    <div class="section-wrapper in-designer-list">
      <div class="text-flex">
        <div class="h2">{{ 'tn25814dc5' | t }}</div>
        <a data-w-id="ed5b8b81-9888-3115-83cf-9192c0bfe4fa" href="{{ pages["designeri"].url }}" class="page-link text-flex w-inline-block">
          <div class="h3 main">{{ 'tn1084d40d' | t }}</div>
          <div data-w-id="ed5b8b81-9888-3115-83cf-9192c0bfe4fd" class="icon-embed-xxsmall arrow-right w-embed"><svg xmlns="http://www.w3.org/2000/svg" width="100%" height="100%" viewBox="0 0 16 16" fill="none" preserveAspectRatio="xMidYMid meet" aria-hidden="true" role="img">
              <path d="M6 12L10 8L6 4" stroke="currentColor" stroke-linecap="round" stroke-linejoin="round"></path>
            </svg></div>
        </a>
      </div>
      {%- render "designi_max_9_sorted_by__random" -%}
    </div>
  </section>
  {%- section 'footer' -%}
  <script src="https://d3e54v103j8qbb.cloudfront.net/js/jquery-3.5.1.min.dc5e7f18c8.js?site=65397d9f3766491d0cad2172" type="text/javascript" integrity="sha256-9/aliU8dGd2tb6OSsuzixeV4y/faTqgFtohetphbbj0=" crossorigin="anonymous"></script>
  <script src="{{ 'webflow.js' | asset_url }}" type="text/javascript" defer></script>
  <style>
  body { -webkit-font-smoothing: antialiased; }
</style>
  <script> document.addEventListener("DOMContentLoaded", function() {
    // Funkce pro zjištění aktuálního času a dne v týdnu uživatele
    function checkTimeAndDay() {
        // Získání aktuálního času uživatele
        var now = new Date();
        // Získání hodin a dnů
        var hour = now.getUTCHours() + 2; // Pro časovou zónu UTC+2 (Praha)
        var day = now.getUTCDay(); // 0-6, kde 0 je Neděle
        // Definice prvků podle class
        var openElement = document.querySelector('.open-time');
        var closedElement = document.querySelector('.closed-time');
        // Kontrola času a dne
        if (day >= 1 && day <= 5 && hour >= 12.5 && hour < 18) {
            // Pokud je otevřeno
            openElement.style.display = 'flex';
            closedElement.style.display = 'none';
        } else {
            // Pokud je zavřeno
            openElement.style.display = 'none';
            closedElement.style.display = 'flex';
        }
    }
    // Spuštění funkce při načtení stránky
    checkTimeAndDay();
    // Volitelně můžete pravidelně aktualizovat stav (např. každou minutu)
    setInterval(checkTimeAndDay, 60000);
});
</script>

{{ 'udesly-shopify.min.js' | asset_url | script_tag }}