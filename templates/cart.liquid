{%- assign headerSnippetPath = "header-search" -%}{%- assign wfPage = "655ff046f6d27cab9f83c7f2" -%}{%- assign bodyClasses = "" -%}
  {%- render 'navigation-1' -%}
  <section class="hero-subpage">
    <div class="section-wrapper in-hero-subpage">
      <div class="text-wrapper display">
        <div class="link-page-wrapper in-pages">
          <a href="{{ routes.root_url }}" class="page-link w-inline-block">
            <div class="text-page-link">{{ 't64e1b1' | t }}</div>
          </a>
          <div class="link-page-text">{{ 'tnec768f6' | t }}</div>
        </div>
        <div class="subpage-hero-grid">
          <h1 id="w-node-_40d6e02e-340c-76e4-a4e7-8d3f27b30579-9f83c7f2" class="heading h1 black">{{ 't458a23b' | t }}</h1>
          <div class="subpage-hero-grid-flex">
            <div class="hero-text-paragraph">{{ 'tn19002407' | t }}</div>
          </div>
        </div>
      </div>
    </div>
  </section>
  <div class="divider in-decor">
    <div class="divider-line"></div>
  </div>
  <div class="form-block w-form">
    <form id="wf-form-Cart" name="wf-form-Cart" data-name="Cart" method="post" class="cart-form" data-wf-page-id="655ff046f6d27cab9f83c7f2" data-wf-element-id="5849b839-cc29-6244-266a-0c3bda32a747" action="{{ routes.cart_url }}">
      <div class="cart-items">
        <p class="paragraph-2">{{ 'tn37f9ac6e' | t }}</p>
        {%- for item in cart.items -%}<div class="cart-item">
          <a href="{{ item.url }}" class="w-inline-block"><img src="{{ item.image | img_url: 'master' }}" loading="lazy" width="254" alt="{{ item.image.alt }}" class="image-10"></a>
          <div class="div-block-2">
            <a href="{{ item.url }}" class="link-block-3 w-inline-block">
              <p class="paragraph-3">{{ item.title }}</p>
            </a>
            <a href="/cart/change?line={{ forloop.index }}&amp;amp;quantity=0" class="link-2">{{ 't36e7193' | t }}</a><input type="number" class="text-field-4 w-input" maxlength="256" name="updates[]" data-name="Field" placeholder="{{ 'tn33b10238' | t }}" id="updates_{{item.key}}" required="" min="0" value="{{item.quantity}}">
            <div class="text-block-18">{{ item.final_line_price | money_with_currency }}</div>
          </div>
        </div>{%- endfor -%}
      </div><input type="submit" value="{{ 'tbcafa41' | t }}" data-wait="Kapku strpení..." class="submit-button w-button" name="update">
      <div class="cart-footer">
        <div id="w-node-_5849b839-cc29-6244-266a-0c3bda32a759-9f83c7f2"><textarea placeholder="{{ 't33920805' | t }}" maxlength="5000" id="note" name="note" data-name="Note" class="textarea w-input">{{ cart.note }}</textarea></div>
        <div id="w-node-_5849b839-cc29-6244-266a-0c3bda32a75b-9f83c7f2" class="cart-totals">
          <div class="cart-lines">
            <div class="cart-line">
              <div class="text-in-total">{{ 'tna813270' | t }}</div>
              <div class="text-in-total">{{ cart.original_total_price | money_with_currency }}</div>
            </div>
            <div class="cart-line">
              <div class="text-in-total">{{ 'tn6c3c26b5' | t }}</div>
              <div class="text-in-total">{{ cart.total_discount | money_with_currency }}</div>
            </div>
          </div>
          <div class="cart-line">
            <div class="text-in-total bigger">{{ 'tn4d39a7a9' | t }}</div>
            <div class="text-in-total">{{ cart.total_price | money_with_currency }}</div>
          </div><input type="submit" value="{{ 't55a4c2a1' | t }}" data-wait="Prosím vyčkejte..." class="submit-button-2 w-button" name="checkout">
        </div>
      </div>
    </form>
    
    
  </div>
  <div class="divider in-decor">
    <div class="divider-line"></div>
  </div>
  {%- section 'footer' -%}
  <script src="https://d3e54v103j8qbb.cloudfront.net/js/jquery-3.5.1.min.dc5e7f18c8.js?site=65397d9f3766491d0cad2172" type="text/javascript" integrity="sha256-9/aliU8dGd2tb6OSsuzixeV4y/faTqgFtohetphbbj0=" crossorigin="anonymous"></script>
  <script src="{{ 'webflow.js' | asset_url }}" type="text/javascript" defer></script>
  <style>
  body { -webkit-font-smoothing: antialiased; }
</style>
  <script> document.addEventListener("DOMContentLoaded", function() {
    // Funkce pro zjištění aktuálního času a dne v týdnu uživatele
    function checkTimeAndDay() {
        // Získání aktuálního času uživatele
        var now = new Date();
        // Získání hodin a dnů
        var hour = now.getUTCHours() + 2; // Pro časovou zónu UTC+2 (Praha)
        var day = now.getUTCDay(); // 0-6, kde 0 je Neděle
        // Definice prvků podle class
        var openElement = document.querySelector('.open-time');
        var closedElement = document.querySelector('.closed-time');
        // Kontrola času a dne
        if (day >= 1 && day <= 5 && hour >= 12.5 && hour < 18) {
            // Pokud je otevřeno
            openElement.style.display = 'flex';
            closedElement.style.display = 'none';
        } else {
            // Pokud je zavřeno
            openElement.style.display = 'none';
            closedElement.style.display = 'flex';
        }
    }
    // Spuštění funkce při načtení stránky
    checkTimeAndDay();
    // Volitelně můžete pravidelně aktualizovat stav (např. každou minutu)
    setInterval(checkTimeAndDay, 60000);
});
</script>

{{ 'udesly-shopify.min.js' | asset_url | script_tag }}